const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');
const root=path.join(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('interactive games are removed from the application root',()=>{const html=read('index.html'),loader=path.join(root,'assets/js/games-loader.js'),engine=path.join(root,'assets/js/games.js'),css=path.join(root,'assets/css/games.css');assert.doesNotMatch(html,/games-loader|games\.css|بازی‌های دونفره|سرگرمی و تعامل/);assert.equal(fs.existsSync(loader),false);assert.equal(fs.existsSync(engine),false);assert.equal(fs.existsSync(css),false)});
test('database cleanup removes every interactive games object',()=>{const sql=read('supabase/migrations/20260914160500_remove_interactive_games.sql');for(const name of['game_submit','game_resign','game_accept','game_create','game_directory','game_matches'])assert.match(sql,new RegExp(name))});
test('desktop home is restored to fixed non-scrolling three-row layout',()=>{const css=read('assets/css/home-stable.css');assert.match(css,/body\.card-home-active\{height:100dvh;overflow:hidden!important/);assert.match(css,/grid-template-rows:repeat\(3,minmax\(0,1fr\)\)!important/);assert.match(css,/#homeFixedFooter\{position:fixed!important/)});
